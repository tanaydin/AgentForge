# Copy to .env and fill in locally. Never commit .env.
# Only architectural concepts are listed; no provider is assumed or required.

APP_ENV=
APP_NAME=

# AI (provider-neutral; e.g. openai | anthropic | google | ollama | local | none)
AI_PROVIDER=
AI_MODEL=

# Persistence (e.g. a connection string once a database is chosen)
DATABASE_URL=

# Source control host (e.g. github | gitlab)
GIT_PROVIDER=

# Project management system (e.g. github-projects | gitlab | jira | linear)
PROJECT_MANAGEMENT_PROVIDER=

# Observability
LOG_LEVEL=
