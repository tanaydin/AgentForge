"""Django ORM models (persistence schema) — the ONLY place ``django.db`` is imported.

Repositories in this package translate rows <-> plain domain objects so that
``src/domain`` and ``src/application`` stay framework-free.

No models yet. Add them, then: python manage.py makemigrations infrastructure
"""
