# tests/

```
tests/
  unit/         isolated, fast, no I/O
  integration/  components together with real adapters where practical
  end_to_end/   full flows through the API/UI
```

No test framework is chosen yet. Add tests alongside implemented behavior, not for
scaffold placeholders. See `docs/development/testing.md`.
