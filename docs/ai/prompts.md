# Prompts

- Prompt templates are version-controlled text, not inline string literals scattered
  through code.
- Repo-level templates: `.ai/prompts/`. Application-level: `src/ai/prompts/`.
- Each template documents its inputs, expected output shape, and intended model role.
- Changes to prompts are reviewed like code.
