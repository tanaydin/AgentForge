# Deployment

No deployment target is chosen. This directory will document environments, build/release
process, rollback strategy, and infrastructure once a platform is selected (each choice
via an ADR).

Environment configuration placeholders live in `config/` (`development`, `testing`,
`production`).
